package com.dancepractice.app.domain.common;

// NOTE: Reference-only enum used for Supabase schema context; no Spring Boot backend is present.

public enum AccountStatus {
  ACTIVE,
  SUSPENDED,
  HIDDEN
}
