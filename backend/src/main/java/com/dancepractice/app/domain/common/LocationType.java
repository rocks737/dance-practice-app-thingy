package com.dancepractice.app.domain.common;

// NOTE: Reference-only enum maintained for Supabase modeling; there is no Spring Boot runtime.

public enum LocationType {
  STUDIO,
  COMMUNITY_SPACE,
  PRIVATE_RESIDENCE,
  OUTDOOR,
  OTHER
}
