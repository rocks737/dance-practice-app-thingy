package com.dancepractice.app.domain.common;

// NOTE: Reference-only enum documenting Supabase visibility values; no Java backend is deployed.

public enum Visibility {
  AUTHOR_ONLY,
  PARTICIPANTS_ONLY,
  PUBLIC
}
